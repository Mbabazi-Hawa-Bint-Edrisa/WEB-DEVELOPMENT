// sum of two numbers 
function calculate_sum(x, y) {
    return x + y;
}


const number_one = 16;
const number_two = 15;
const sum = calculate_sum(number_one, number_two);
console.log(`The sum of ${number_one} and ${number_two} is: ${sum}`);
